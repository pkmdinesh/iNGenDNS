package com.ingendns.app.core.constants

object AppConstants {

    const val APP_NAME = "iNGenDNS"

    const val DEFAULT_DNS_TEST_INTERVAL = 30 * 60L

    const val DEFAULT_MONITOR_INTERVAL = 30 * 60L

    const val DEFAULT_SWITCH_DELAY = 30L

    const val DEFAULT_RETRY_COUNT = 5

    const val MAX_CUSTOM_DNS = 3

    const val DNS_LIST_FILE = "DNS_LIST.txt"

    const val DNS_RESULT_FILE = "DNS_RESULT.txt"

}
